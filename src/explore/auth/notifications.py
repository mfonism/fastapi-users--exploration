async def send_verification_request(
    *,
    recipient_email: str,
    recipient_name: str,
    token: str,
) -> None:
    # Placeholder until an email provider is wired in.
    pass


async def send_password_reset_request(
    *,
    recipient_email: str,
    recipient_name: str,
    token: str,
) -> None:
    # Placeholder until an email provider is wired in.
    pass


async def send_email_change_request(
    *,
    recipient_email: str,
    recipient_name: str,
    token: str,
) -> None:
    # Placeholder until an email provider is wired in.
    pass


async def send_reactivation_request(
    *,
    recipient_email: str,
    recipient_name: str,
    token: str,
) -> None:
    # Placeholder until an email provider is wired in.
    pass
