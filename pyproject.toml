[project]
name = "explore"
version = "0.1.0"
description = "A project for exploring the library `fastapi-users`"
readme = "README.md"
authors = [
    { name = "Mfon Eti-mfon", email = "mfonetimfon@gmail.com" }
]
requires-python = ">=3.14.3,<3.15"
dependencies = [
    "asyncpg>=0.30.0",
    "fastapi-users[redis,sqlalchemy]>=15.0.3",
    "fastapi[standard]>=0.128.0",
    "packaging>=25.0",
    "pydantic-settings>=2.11.0",
    "psycopg[binary]>=3.2.10",
    "python-dotenv>=1.1.1",
    "alembic[tz]>=1.18.4",
]

[project.scripts]
db-bootstrap = "explore.db.bootstrap:main"
db-upgrade = "explore.db.migrate:upgrade_main"
db-downgrade = "explore.db.migrate:downgrade_main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "pytest-mock>=3.15.1",
    "ruff>=0.14.14",
]

[tool.ruff]
line-length = 88
target-version = "py314"

[tool.ruff.lint]
select = ["BLE", "E", "F", "I", "T"]
fixable = ["E", "F", "I"]
